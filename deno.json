{
  "tasks": {
    "build": "deno compile -A fastadd.ts",
    "test": "deno run -A fastadd.ts",
    "install": "deno task build && mkdir -p \"$HOME/.local/bin\" && mv fastadd \"$HOME/.local/bin\""
  }
}
