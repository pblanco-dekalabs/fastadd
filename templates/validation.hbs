import { TypedRouteReply, TypedRouteRequest } from "@/routes/types"
import { baseResponseSchema } from "@/server/config/response"

export const {{schemaName}} = {
  $id: "{{schemaId}}",
  type: "object",
  properties: { // TODO: Populate the schema
    id: { type: "string" },
  },
  additionalProperties: false,
  required: ["id"],
} as const
{{#each info.children}}

export const {{camelCase this.name}} = {
  operationId: "{{camelCase this.name}}",
  description: "TODO",
  tags: [],
  response: {
    200: baseResponseSchema({
      // TODO: Add the true schema
    })
  },
  security: [{{#if this.needsAuth}}{ bearerAuth: [] }{{/if}}]
} as const

export type {{pascalCase this.name}}Request = TypedRouteRequest<typeof {{camelCase this.name}}>
export type {{pascalCase this.name}}Reply = TypedRouteReply<typeof {{camelCase this.name}}>

{{/each}}
