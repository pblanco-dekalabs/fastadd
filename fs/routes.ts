import { path } from "../deps.ts";

export const ROOT_DIR = path.join('apps', 'api', 'src')
export const SERVICES_DIR = path.join(ROOT_DIR, 'domain', 'services')
export const REPOSITORIES_DIR = path.join(ROOT_DIR, 'domain', 'repositories')
export const CONTROLLERS_DIR = path.join(ROOT_DIR, 'controllers')
export const ROUTES_DIR = path.join(ROOT_DIR, 'routes')
